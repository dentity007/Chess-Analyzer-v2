import { platformClient } from './platformClient';

export const Game = platformClient.entities.Game;
export const Analysis = platformClient.entities.Analysis;
export const PlayerStats = platformClient.entities.PlayerStats;

export const User = platformClient.auth;